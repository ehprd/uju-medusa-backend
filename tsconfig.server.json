{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    /* Emit product_variant.ts single file with source maps instead of having product_variant.ts separate file. */
    "inlineSourceMap": true
  },
  "exclude": ["src/admin", "**/*.spec.js"]
}
